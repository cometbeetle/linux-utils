# Root Tools Toolbox

## Important Notes

- Run `distrobox assemble create` from within **this directory**, otherwise the
  setup commands will fail.
- This is a rooted container, which may make your system more vulnerable to attacks.
